# Open task write-ups — 2026-07-06

Solution plans for every open GitHub issue and every finding in the
`fable-review` branch's `docs/2026-07-02-fable-review.md`. Each plan was
written against verified current code on `main` @ 4152307 (a background pass
re-located every referenced symbol; line numbers below are current).

## Open GitHub issues

| Doc | Issue | Summary |
|---|---|---|
| `2026-07-06-gh-issue-113.md` | #113 | Parse Venice `aci/1` attestation format (new backend format + report-data verifier + fixtures). |
| `2026-07-06-gh-issue-117.md` | #117 | Fix `response_schema` chicken-egg by making conditional fields (`nvswitch`) structurally optional; enforcement moves to the topology-aware factor. |
| `2026-07-06-gh-issue-118.md` | #118 | Add a Tinfoil supply-chain policy; make policy mandatory at all validation callsites (close the nil-policy bypass). |
| `2026-07-06-gh-issue-119.md` | #119 | Make `tee_debug_disabled` a dedicated evaluator (it already exists inline) and add a hazardous-config-bits factor set. |
| `2026-07-06-gh-issue-120.md` | #120 | Remove the `TinfoilSupplyChainResult` scalar `ComponentRepos` fallback; strengthen empty-`Components` to fail closed. |
| `2026-07-06-gh-issue-124.md` | #124 | Reasoning loss: extend PR #123 diagnostics/repair to `/v1/responses`, broaden the model-family flag map, add model aliases (root cause), opt-in prompt-sandwich merge, live verification. |

## fable-review findings

| Doc | Finding | Summary |
|---|---|---|
| `2026-07-06-fable-review-h1.md` | H1 (High) | Pointer to the existing full plan (`docs/code-review/2026-07-02-h1-fix-plan.md`) + verified state. Tinfoil cloud (SEV-SNP) must not exempt quote signature/cert chain. |
| `2026-07-06-fable-review-m1.md` | M1 | nanogpt/phalacloud freshness+binding exemptions → explicit weak-tier opt-in; harden `--update-config` pinning. |
| `2026-07-06-fable-review-m2.md` | M2 | Venice/NearCloud response decryption not bound to attested key — probe ground truth, then pin or protocol-change. |
| `2026-07-06-fable-review-m3.md` | M3 | SPKI re-pinning skipped on attestation cache hits — cache the fingerprint, verify every response. **Do first.** |
| `2026-07-06-fable-review-m4.md` | M4 | DNS-rebinding: Host-header allowlist + security headers on all routes. |
| `2026-07-06-fable-review-m5.md` | M5 | Dashboard attribute-injection XSS — DOM-API rendering, fixed `esc()`, CSP, server-side model-id validation. |
| `2026-07-06-fable-review-m6.md` | M6 | `--update-config` erases explicit `allow_fail = []` — pointer fields, strict decode. |
| `2026-07-06-fable-review-low-findings.md` | L1–L15 | All fifteen lows in one doc, grouped by theme, with sequencing. |

## Suggested order

1. **H1** (existing plan) — the one High; two review passes converged on it.
2. **M3** — TLS binding is bypassed on every cache hit today.
3. **M6** then **M1** — both touch `update.go`; M6 is smaller.
4. **M4 + M5** — land together (Host check removes M5's amplification).
5. **M2**, **L3**, **L15** — provider-coordinated; confirm server behavior first.
6. Issues #120, #117, #119 are self-contained refactors/hardening.
7. #118 interlocks with L8; #113 with #118's policy shape.

## Cross-cutting notes

- Several fixes converge in `internal/config/update.go` (M1, M6) and on the
  supply-chain policy (#118, #120, L8) — sequence to avoid conflicts.
- H1 is a **prerequisite for trusting** #119's debug bit and every
  measurement factor: the bits are only as good as the quote signature.
- Golden `report.txt` fixtures use stale `tdx_*` factor names vs. current
  `tee_*` constants — regenerate during #119.
- Every plan honors AGENTS.md: fail closed, no fallbacks, strict parsing,
  constant-time crypto compares, gocyclo ≤ 32 via up-front decomposition,
  concurrent `-race` tests for shared state, regression tests for each
  finding, and no audit identifiers in code/commits.
